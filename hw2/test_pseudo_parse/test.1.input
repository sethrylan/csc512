VAR a,b,c:INT;
BEGIN
c:=thx[1138]*INT(10);
a:=-1.0+0.20/0.1;
IF b>1 AND b<>b OR NOT b=b THEN WRITE(a); ELSE READ(a); ENDIF;
IF z>=0.45 AND zz<>12.0 OR zzz[z]=-1.2 OR z<-0.2 OR z>-2.0 THEN 
ENDIF;
a:=110/10;
END
